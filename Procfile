web: python app.py
``>

※注意：Procfileは「拡張子なし」のプレーンテキストです。

---

## 🖥 3. ターミナルでGitコマンドを実行する

### ✅ 方法①：「Git Bash」使用（おすすめ）

1. 作成した `命式API` フォルダを**右クリック**  
2. メニューから「Git Bash Here」を選択  
3. 以下のコマンドを1行ずつ貼り付けてEnterします👇

```bash
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/himayuri1717/meishiki-api.git
git push -u origin main
